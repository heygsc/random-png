name: Generate Large Random Image

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Generate large image
        run: yarn generate:large

      - name: Commit generated large image
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions@github.com"
          git add random-large.png
          git diff --cached --quiet || git commit -m "chore: generate large random image"
          git push
